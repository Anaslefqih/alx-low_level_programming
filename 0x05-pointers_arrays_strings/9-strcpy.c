#include "main.h"

/**
 * _strcpy - copies the string pointed to by src
 * @dest: The destination
 * @src: The source
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	int a;

	for (a = 0; src[a]; a++)
	{
		dest[i] = src[a];
	}
	dest[a] = '\0';
	return (dest);
}
